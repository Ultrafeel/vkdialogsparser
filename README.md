# VK Dialogs and Posts Parser

Расширенный дампер диалогов и постов сообществ из ВКонтакте с поддержкой многопоточности и экспорта в JSON/HTML форматы.

## 🆕 Новые возможности (после upstream)

### 🔗 **Парсинг VK-ссылок**
- Автоматическое преобразование VK-ссылок формата `[#alias|текст|url]` в HTML
- Поддержка в постах, комментариях, пересланных сообщениях и диалогах
- Безопасность: экранирование HTML для предотвращения XSS
- Ссылки открываются в новых вкладках

### 🖼️ **Кеширование изображений**
- Автоматическая загрузка и кеширование всех изображений
- Уникальные имена файлов на основе MD5 хешей URL
- Создание папки кеша рядом с HTML файлом
- Fallback механизм: кешированные изображения + оригинальные URL как резерв
- Поддержка фото вложений, превью видео и кадров

### 📋 **Поддержка пересланных постов (copy_history)**
- Полная поддержка отображения пересланных постов
- Все типы вложений в пересланных постах
- Единообразное отображение с основными постами
- Работает как в статическом HTML, так и в интерактивном просмотрщике

### 🌐 **Интерактивный JSON просмотрщик**
- `json_viewer.html` - автономный просмотрщик JSON файлов
- Автоматическое обнаружение JSON файлов в папке
- Ручной выбор файлов через браузер
- Поиск в реальном времени по всему контенту
- Точное соответствие стилей с HTML экспортом

### 🧪 **Тестирование и валидация**
- `test_vk_links.py` - тесты парсинга VK-ссылок
- `test_image_cache.py` - тесты кеширования изображений
- Автоматическая проверка корректности обработки
- Примеры использования всех новых функций

## Возможности

### ✨ Основные функции
- 📱 **Выгрузка диалогов** - сохранение переписок с пользователями, группами и чатами
- 📝 **Выгрузка постов сообществ** - полная выгрузка постов из публичных групп/сообществ
- 🎯 **Гибкие режимы работы** - диалоги, посты или оба одновременно
- 📄 **Множественные форматы** - JSON и HTML экспорт
- ⚡ **Многопоточность** - быстрая обработка больших объемов данных
- 🔧 **Современная конфигурация** - .env файлы с fallback на input

### 📊 Поддерживаемые данные
- Текст сообщений и постов с VK-ссылками
- Все типы вложений (фото, видео, документы, стикеры) с кешированием
- Комментарии к постам
- Лайки и реакции
- Пересланные сообщения и посты (copy_history)
- Метаданные (даты, авторы, статистика)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd vkdialogsparser
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл конфигурации:
```bash
cp .env.example .env
```

4. Отредактируйте `.env` файл с вашими настройками.

## Конфигурация

### Основные параметры (.env файл)

```env
# VK API Configuration
VK_TOKEN=your_vk_token_here
VK_APP_ID=your_app_id

# Operation Mode
DUMP_MODE=dialogs  # dialogs, posts, both
EXPORT_FORMAT=json,html  # json, html, or both

# Threading
THREAD_COUNT=4

# Output Configuration
OUTPUT_DIRECTORY=output
DIALOGS_DIRECTORY=dialogs
POSTS_DIRECTORY=posts

# Community Posts Configuration (when DUMP_MODE=posts or both)
VK_COMMUNITY_ID=your_community_id
POSTS_COUNT=100
INCLUDE_COMMENTS=true
INCLUDE_REACTIONS=true

# Dialog Configuration (when DUMP_MODE=dialogs or both)
MAX_DIALOGS=200
```

### Получение VK токена

1. Перейдите на [vkhost.github.io](https://vkhost.github.io/)
2. Авторизуйтесь через ВКонтакте
3. Скопируйте полученный токен в параметр `VK_TOKEN`

## Использование

### Запуск с .env конфигурацией
```bash
python main.py
```

### Запуск с переменными окружения
```bash
# Только диалоги
DUMP_MODE=dialogs python main.py

# Только посты сообщества
VK_COMMUNITY_ID=12345 DUMP_MODE=posts python main.py

# Оба режима с HTML экспортом
DUMP_MODE=both EXPORT_FORMAT=json,html python main.py
```

### Интерактивный режим
Если параметры не заданы в .env файле, программа запросит их интерактивно:
- VK токен (обязательно)
- Режим работы
- Количество потоков
- ID сообщества (для постов)
- Другие настройки

## Примеры использования

### Выгрузка диалогов
```bash
# Настройте .env:
DUMP_MODE=dialogs
EXPORT_FORMAT=json,html
MAX_DIALOGS=50

# Запустите:
python main.py
```

### Выгрузка постов сообщества
```bash
# Настройте .env:
DUMP_MODE=posts
VK_COMMUNITY_ID=1  # ID группы ВКонтакте (например, для vk.com/apiclub)
POSTS_COUNT=200
INCLUDE_COMMENTS=true
EXPORT_FORMAT=json,html

# Запустите:
python main.py
```

### Полная выгрузка
```bash
# Настройте .env:
DUMP_MODE=both
VK_COMMUNITY_ID=1
EXPORT_FORMAT=json,html

# Запустите:
python main.py
```

## Структура выходных файлов

```
output/
├── dialogs/
│   ├── User_Name.json
│   ├── User_Name.html
│   ├── User_Name_files/          # 🆕 Кеш изображений для диалогов
│   │   ├── abc123def.jpg
│   │   └── xyz789uvw.png
│   ├── Group_Name.json
│   ├── Group_Name.html
│   └── Group_Name_files/         # 🆕 Кеш изображений для групп
│       └── cached_images...
├── posts/
│   ├── Community_Name_posts.json
│   ├── Community_Name_posts.html
│   └── Community_Name_posts_files/  # 🆕 Кеш изображений для постов
│       ├── photo_hash1.jpg
│       ├── video_thumb_hash2.jpg
│       └── video_frame_hash3.jpg
└── json_viewer.html              # 🆕 Интерактивный просмотрщик
```

### 🆕 Кеширование изображений
Для каждого HTML файла создается соответствующая папка `*_files/` с кешированными изображениями:
- Уникальные имена на основе MD5 хеша URL
- Автоматическое определение расширения файла
- Fallback на оригинальные URL при недоступности кеша
- Комментарии в HTML с оригинальными URL для справки

### JSON формат (диалоги)
```json
{
  "title": "Имя диалога",
  "peer_id": 123456,
  "type": "user",
  "messages": [
    {
      "message_id": 1,
      "date": 1640995200,
      "from_id": 123456,
      "text": "Текст сообщения",
      "attachments": [...],
      "fwd_messages": [...]
    }
  ]
}
```

### JSON формат (посты)
```json
{
  "type": "community_posts",
  "export_date": "2024-01-01T12:00:00",
  "community": {
    "id": -12345,
    "name": "Название сообщества",
    "members_count": 10000
  },
  "posts": [
    {
      "id": 123,
      "date": 1640995200,
      "text": "Текст поста",
      "attachments": [...],
      "comments": [...],
      "likes": {...}
    }
  ]
}
```

## HTML экспорт

HTML файлы включают:
- 🎨 Современный responsive дизайн
- 🔍 Поиск по содержимому
- 📱 Адаптивная верстка для мобильных устройств
- 🖼️ **Кешированные изображения** с автоматическим fallback
- 💬 Развернутые комментарии с VK-ссылками
- 📊 Статистика (лайки, репосты, просмотры)
- 🔗 **Автоматическое преобразование VK-ссылок** в кликабельные ссылки
- 📋 **Полная поддержка пересланных постов** со всеми вложениями
- 🎬 **Расширенное отображение видео** с превью и кадрами

## 🌐 JSON Viewer

Интерактивный просмотрщик `json_viewer.html`:
- 📁 **Автоматическое обнаружение** JSON файлов в папке
- 🔧 **Ручной выбор файлов** через браузер
- 🔍 **Поиск в реальном времени** по всему контенту
- 🎨 **Точное соответствие** стилей с HTML экспортом
- 📱 **Поддержка всех форматов**: посты, диалоги, общий JSON
- 🖼️ **Все новые функции**: VK-ссылки, кеширование, copy_history

### Использование JSON Viewer
1. Поместите `json_viewer.html` в папку с JSON файлами
2. Откройте файл в браузере
3. Выберите JSON файл из списка или загрузите вручную
4. Используйте поиск для навигации по контенту

## 🧪 Тестирование новых функций

### Тестирование парсинга VK-ссылок
```bash
python test_vk_links.py
```
Этот тест проверяет:
- Корректность преобразования `[#alias|текст|url]` в HTML ссылки
- Экранирование HTML символов для безопасности
- Обработку множественных ссылок в одном тексте
- Игнорирование неправильных форматов
- Обработку пустых и null значений

### Тестирование кеширования изображений
```bash
python test_image_cache.py
```
Этот тест проверяет:
- Создание папки кеша рядом с HTML файлом
- Загрузку и сохранение изображений с уникальными именами
- Генерацию HTML с ссылками на кешированные файлы
- Fallback механизм при недоступности изображений
- Комментарии с оригинальными URL в HTML

### Проверка всех новых функций
Для полной проверки функциональности:
1. Создайте тестовые данные с VK-ссылками и изображениями
2. Запустите экспорт в HTML формате
3. Проверьте создание папок `*_files/` с кешированными изображениями
4. Откройте HTML файлы и убедитесь в корректном отображении:
   - VK-ссылки преобразованы в кликабельные ссылки
   - Изображения загружаются из кеша с fallback на оригинальные URL
   - Пересланные посты отображаются со всеми вложениями
   - Видео показывают превью и кадры
5. Используйте `json_viewer.html` для интерактивного просмотра JSON файлов

## 🔧 Новые возможности в действии

### Пример VK-ссылки в тексте
**Исходный текст в VK:**
```
Проверьте эту статью: [#alias|example.com/article|https://example.com/full-article-url]
```

**Результат в HTML:**
```html
Проверьте эту статью: <a href="https://example.com/full-article-url" target="_blank">example.com/article</a>
```

### Пример структуры кеша изображений
**После экспорта поста с изображениями:**
```
Community_posts.html
Community_posts_files/
├── a1b2c3d4e5f6.jpg    # Фото из поста
├── f6e5d4c3b2a1.jpg    # Превью видео
└── 9z8y7x6w5v4u.jpg    # Кадр из видео
```

**В HTML файле:**
```html
<img src="Community_posts_files/a1b2c3d4e5f6.jpg" 
     onerror="this.src='https://original-vk-url.com/image.jpg'">
<!-- Original URL: https://original-vk-url.com/image.jpg -->
```

### Пример пересланного поста
**В JSON данных:**
```json
{
  "text": "Смотрите этот пост:",
  "copy_history": [
    {
      "text": "Оригинальный текст поста",
      "attachments": [...],
      "date_formatted": "01.01.2024 12:00:00"
    }
  ]
}
```

**Результат в HTML:**
- Основной пост: "Смотрите этот пост:"
- Блок "🔄 Репост оригинала:" с полным содержимым оригинального поста
- Все вложения оригинального поста отображаются корректно

## Устранение неполадок

### Ошибки авторизации
- Проверьте правильность VK токена
- Убедитесь, что токен имеет необходимые права доступа
- Попробуйте получить новый токен

### Ошибки доступа к сообществу
- Убедитесь, что сообщество публичное
- Проверьте правильность ID сообщества
- Некоторые сообщества могут ограничивать доступ к API

### Проблемы с производительностью
- Уменьшите количество потоков (`THREAD_COUNT`)
- Ограничьте количество обрабатываемых элементов
- Используйте только JSON экспорт для больших объемов данных

## Технические детали

### Архитектура
- `main.py` - точка входа и оркестрация
- `config/settings.py` - управление конфигурацией
- `modules/dialogs.py` - обработка диалогов с поддержкой новых функций
- `modules/posts.py` - обработка постов сообществ с VK-ссылками и кешированием
- `json_viewer.html` - интерактивный просмотрщик JSON файлов
- `test_vk_links.py` - тесты парсинга VK-ссылок
- `test_image_cache.py` - тесты кеширования изображений

### Новые функции в modules/posts.py
- `parse_vk_links()` - парсинг VK-ссылок формата `[#alias|текст|url]`
- `download_image()` - загрузка и кеширование изображений
- `get_cached_image_path()` - получение пути к кешированному изображению с fallback
- `render_video_attachment()` - расширенное отображение видео с кешированием
- `render_attachments()` - универсальная обработка всех типов вложений
- Поддержка `copy_history` (пересланных постов) во всех функциях

### Зависимости
- `vk_api` - взаимодействие с VK API
- `python-dotenv` - загрузка .env файлов
- `requests` - загрузка изображений для кеширования (>=2.25.0)
- `hashlib` - генерация уникальных имен для кешированных файлов
- `urllib.parse` - обработка URL для кеширования

### Ограничения VK API
- Максимум 5000 запросов в день для пользовательских токенов
- Ограничения на частоту запросов (3 запроса в секунду)
- Некоторые данные могут быть недоступны из-за настроек приватности

## Лицензия

Этот проект распространяется под лицензией MIT. См. файл LICENSE для подробностей.

## Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение неполадок"
2. Убедитесь, что используете актуальную версию
3. Создайте issue с подробным описанием проблемы
